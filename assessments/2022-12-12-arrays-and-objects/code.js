const makeRow = () => {
  return [' ', ' ', ' ']
}

const makeBoard = () => {
  return [[' ', ' ', ' '], [' ', ' ', ' '], [' ', ' ', ' ']]
}

const makeMove = (s, num1, num2) => {
  return //what does this mean i literally don't undestand this at all 
  //what's a mark 
}

const placeMark = (array, obj) => {
  return makeBoard 
  //w hat does this meeeean 
}

const allTheSame = ([a, b, c]) => {
  return (a === b) && (a === c)
}

const extractColumn = (array, num) => {
  return [array(0.num), array(1,num), array,2, num]
}
//agh you get the idea, i just don't know the right way to get one element
const array = 
[
     ['X', '', ''],
     ['O', '', ''],
     ['', 'X', '']
   ]